apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: env-team-c
  namespace: flux-system
spec:
  interval: 1m
  releaseName: env-team-c
  targetNamespace: env-team-c
  chart:
    spec:
      chart: vcluster
      version: 0.22.0
      sourceRef:
        kind: HelmRepository
        name: loft-sh
  install:
    createNamespace: true
  values:
    sync:
      extraArgs:
        - --tls-san=env-team-c.env-team-c.svc.cluster.local
        - --out-kube-config-server=https://env-team-c.env-team-c.svc.cluster.local

