apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: env-team-a
  namespace: env-team-a
spec:
  interval: 10m0s
  prune: true
  sourceRef:
    kind: GitRepository
    namespace: flux-system
    name: flux-system
  path: "./env/team-a"
  dependsOn:
    - name: vcluster-team-a
      namespace: flux-system
  kubeConfig:
    secretRef:
      name: vc-env-team-a
      key: config

